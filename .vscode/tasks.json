{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "activateVenv",
            "type": "shell",
            "command": "${workspaceFolder}/venv/Scripts/activate",
            "options": {
                "env": {
                    "PYTHONPATH": "${workspaceFolder}"
                }
            },
            "problemMatcher": []
        }
    ]
}